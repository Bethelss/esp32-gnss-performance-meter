# Как устроен проект (заметки для владельца)

Этот файл — «живой» документ. Я буду дописывать сюда по мере усложнения
проекта всё, что стоит знать: архитектуру, как связаны модули, какие есть
подводные камни. Читай сверху вниз — новые разделы добавляются внизу.

## Этап 1: тестовая прошивка (bring-up)

### Что сделано

Создан минимальный ESP-IDF проект, который проверяет железо:

1. **ESP32-S3 стартует** — в начале печатаем в консоль: модель чипа,
   размер flash (16 МБ), есть ли PSRAM (8 МБ, октальный режим), пины.
2. **I2C шина + OLED** — при старте сканируем шину (GPIO8=SDA, GPIO9=SCL,
   400 кГц), печатаем все найденные адреса, инициализируем дисплей.
3. **GNSS UART** — UART1, 38400 8N1, RX=GPIO18, TX=GPIO17. Только читаем,
   **ничего не отправляем** модулю — сначала проверяем заводскую конфигурацию.
4. **PPS** — GPIO4, прерывание по фронту (0→1), считаем импульсы и
   запоминаем время последнего (esp_timer_get_time(), микросекунды).

Собрано без ошибок: `gnss_logger_test.bin`. Прошивается так:

    build_idf.bat -p COM5 flash monitor     (COM5 заменить на свой порт)

### Архитектура

Всего две задачи FreeRTOS — больше не нужно:

    GNSS UART (драйвер, кольцевой буфер 2 КБ)
        |
        v
    Задача gnss_rx  (приоритет 10 — высокий)
        |
        +--> печать NMEA-строк в консоль (ESP_LOGI)
        |
        +--> разбор GGA (фикс, спутники) и RMC (валидность, скорость)
        |
        +--> счётчик UBX-пакетов (по сигнатуре B5 62)
        |
        v
    структура статуса (защищена мьютексом)
        |
        v
    Задача display  (приоритет 5, обновление ~7 Гц)
        |
        +--> OLED 128x64 по I2C

Ключевые принципы:

- **GNSS-задача никогда не ждёт дисплей.** Она читает UART блоками до
  256 байт и сразу скармливает байты парсеру. Дисплей медленный (I2C),
  но он живёт в другой задаче с низким приоритетом.
- **Байты GNSS не теряются.** Драйвер UART держит внутренний кольцевой
  буфер на 2048 байт: пока дисплей рисует, приёмщик UART копит байты
  сам, на уровне железа/прерываний, независимо от наших задач.
- Обмен между задачами — через одну структуру `gnss_status_t` под
  мьютексом. Дисплей раз в 150 мс делает копию и рисует её. Никаких
  динамических выделений памяти в циклах.

### Как мы общаемся с GNSS-модулем

Модуль (Quescan G10A-F30, семейство u-blox M10) говорит по UART.
Физика простая:

- У модуля есть ножка **T** — это его передатчик (TX). Она подключена к
  **GPIO18** — вход UART1 нашего ESP32. То, что модуль «говорит»,
  мы слушаем.
- Ножка **R** модуля (его приёмник) подключена к **GPIO17** — выход
  UART1 ESP32. Мы туда пока *ничего* не пишем: если бы случайно
  отправили конфигурацию, модуль запомнил бы её во флеше, и заводские
  настройки мы бы потеряли.
- Настройки линии: 38400 бод, 8 бит данных, без чётности, 1 стоп-бит,
  без аппаратного управления потоком — это заводские значения M10.

Модуль выдаёт смесь данных двух форматов, и прошивка различает их
на лету, байт за байтом:

- **NMEA** — текстовые строки вида `$GNGGA,123519.00,...`. Они начинаются
  с `$` и кончаются переводом строки. Мы собираем такие строки в буфер
  и печатаем целиком в консоль — так ты видишь «сырой» трафик глазами.
- **UBX** — бинарные пакеты u-blox, начинаются с двух байт `0xB5 0x62`.
  Сейчас мы их только **считаем**: ловим сигнатуру, читаем заголовок
  (класс, ID, длина), пропускаем тело и увеличиваем счётчик. На экран
  идёт «UBX: YES/NO» и количество. Это доказательство, что бинарный
  канал жив, — полезно для следующего этапа (UBX-NAV-PVT).

Из NMEA пока разбираем ровно то, что нужно для теста:
- `GGA` → качество фикса (0 = нет фикса) и число спутников;
- `RMC` → статус A/V (валидное место или нет) и скорость в узлах,
  которую переводим в км/ч (×1,852).

Итого на экране: есть ли данные вообще, есть ли фикс, сколько спутников,
скорость, счётчики NMEA/UBX, PPS. Дома без антенны «NO FIX» — это норма,
а не поломка.

### Как мы рисуем на дисплее

Дисплей — дешёвая китайская матрица 1.3" 128×64, монохромная, на шине
I2C (тот самый «SCK» на плате — это на самом деле I2C-линия SCL).

**Контроллер неизвестен** (на модуле только надпись «1.30 IIC»). Для
таких модулей почти всегда это **SH1106**, иногда SSD1306. Они
совместимы на 95%, но есть две ловушки:

1. У SH1106 внутриэкранная память шире видимой области (132 столбца
   вместо 128). Поэтому при отрисовке мы всегда ставим начальный
   столбец **+2** — иначе текст уезжает на 2 пикселя влево и первый
   столбец букв отрезается.
2. Последовательности инициализации немного разные (зарядовый насос,
   предзаряд, DC-DC).

В `display.c` реализованы обе последовательности, переключение — одной
константой/define'ом (по умолчанию SH1106). Как отличить по симптому:
если текст **сдвинут на 2 пикселя влево** — значит физически стоит
SH1106, а мы его рисуем как SSD1306 (или наоборот, сдвиг вправо).
Если экран совсем пустой при успешной инициализации — просто
переключи контроллер на второй вариант, это одна строка в
`main/CMakeLists.txt` (описано в README).

**Как происходит рисование.** Вся картинка — это массив-фреймбуфер
128×64 байт (1 КБ) в обычной RAM. Физика матрицы такая: она разделена
на 8 «страниц» по 8 пикселей высотой; каждый байт — это 8 вертикальных
пикселей одного столбца. Мы рисуем текст своим встроенным шрифтом 5×8
(таблица 96 символов ASCII, каждой букве — 5 байт-столбиков), складываем
всё в фреймбуфер, а затем за один проход выталкиваем весь буфер в
контроллер: для каждой из 8 страниц — команда «столбец 2» (offset SH1106)
и 128 байт данных по I2C.

Порядок обновления раз в цикле дисплея (каждые ~150 мс):
1. Скопировали статус GNSS и PPS (под мьютексом, быстро).
2. Очистили фреймбуфер в RAM.
3. Нарисовали 8 строк текста (это чистое CPU, мгновенно).
4. Выгрузили буфер на дисплей (вот тут ~1–2 мс на страницу по I2C,
   итого несколько мс при 400 кГц).

Никаких внешних библиотек — драйвер, шрифт и фреймбуфер написаны руками
на ESP-IDF API (`driver/i2c_master.h`), поэтому проект самодостаточен и
легко читается. Когда дойдёт до «красивого» интерфейса, можно будет
подключить U8g2 через компонент-менеджер (`idf.py add-dependency`),
архитектура это позволяет.

### Что докладывать после первого запуска

- Экран пустой → пришли строки `display:` из консоли (нашёлся ли 0x3C,
  прошла ли инициализация).
- Нет I2C-устройств → проверь SDA/SCL местами, питание 3В3.
- Нет UART-данных → `RX: NONE (0 B)`; проверь, что T модуля идёт на
  GPIO18.
- Текст сдвинут → фото экрана + какой контроллер выбран.
- NMEA есть, фикса нет → нормально дома; пришли число спутников.

---

## Этап 2: UBX-NAV-PVT и управление приёмником (итерация 2)

Цель этапа — научиться полноценно управлять u-blox M10 и читать его
бинарный протокол UBX. NMEA остаётся только как диагностический канал.

### Что появилось нового

Структура кода изменилась — GNSS вынесен в подпапку `main/gnss/`:

    main/
        main.c              ППС, диагностика, задача дисплея (3 страницы)
        display.c/.h        SH1106/SSD1306, 3 диагностические страницы
        gnss/
            gnss.c/.h       задача приёма + разбор фреймов + статус решения
            ubx_parser.c/.h НЕЗАВИСИМЫЙ парсер UBX-потока (автомат состояний)
            ubx_protocol.c/.h отправка UBX + контрольные суммы + ACK/NAK
            ubx_config.c/.h MON-VER, CFG-VALGET/VALSET, NAV-PVT, NAV-SAT, iTOW

### Парсер UBX (автомат состояний)

Раньше UBX только «считался». Теперь полноценный парсер, который:
- собирает фреймы из произвольных кусков UART (пакет может быть разбит
  между чтениями, несколько пакетов в одном чтении — всё обрабатывается);
- проверяет контрольную сумму (Fletcher-16) каждого пакета;
- отбрасывает абсурдные длины (>512 байт полезной нагрузки);
- восстанавливается после мусора (счётчик resync);
- параллельно собирает NMEA-строки (они остаются в потоке).

Счётчики: rx_bytes, ubx_packets_ok, ubx_checksum_errors,
ubx_length_errors, nmea_sentences, parser_resyncs. Они видны на
странице DIAGNOSTICS и в консоли раз в секунду.

### Отправка команд модулю (первый раз!)

Теперь мы отправляем UBX-пакеты: `ubx_send()` строит заголовок,
кодирует длину little-endian, считает CK_A/CK_B и пишет в UART.
Для CFG-команд реализовано ожидание UBX-ACK-ACK / ACK-NAK с таймаутом:
отличаются «принято» (ACK), «отклонено» (NAK) и «молчание» (timeout).

### Что происходит при старте (после первых байт из модуля)

1. **PHASE B** — MON-VER: спрашиваем версию прошивки/железа/протокола.
   Всё печатается в консоль (swVersion, hwVersion, PROTVER=..., MOD=...).
2. **PHASE B** — CFG-VALGET: читаем текущую конфигурацию из RAM-слоя:
   baud, протоколы входа/выхода, период измерений, нав. коэффициент,
   включённые созвездия, частоты сообщений (NAV-PVT, GGA, RMC и т.д.).
   Печатается сводка «GNSS configuration:».
3. **PHASE C** — CFG-VALSET **только в RAM-слой** (volatile!): включаем
   NAV-PVT на UART1. NMEA остаётся включённым, baud остаётся 38400.
4. Ждём первый NAV-PVT → PHASE D: полный разбор пакета.

### Почему RAM-слой, и почему это важно

UBX-конфигурация хранится в слоях: RAM (текущая работа), BBR
(батарейное), Flash (постоянная). Мы пишем **только в RAM** (layer=0x01
в CFG-VALSET). Значит после отключения питания модуль вернётся к
заводским настройкам — испортить его экспериментами нельзя.

### NAV-PVT — главный источник данных

Один пакет = одно навигационное решение (92 байта по официальной
структуре M10). Разбор — явными little-endian хелперами (read_u32_le и
т.п.), без наложения C-структуры. Внутри всё хранится в родных единицах:
- скорость gSpeed — мм/с (в km/h переводим только при выводе: ×0.0036);
- широта/долгота — 1e-7 градуса;
- точности hAcc/vAcc — мм, sAcc — мм/с;
- pDOP — 0.01.
Скорость берём ИЗ gSpeed, а не вычисляем из разницы координат.

### Реальная частота — по iTOW, не по вере

iTOW — время недели GPS навигационной эпохи, в мс. Сравнивая соседние
iTOW: дельта 1000 мс = 1 Гц, 100 мс = 10 Гц, 50 мс = 20 Гц, 40 мс =
25 Гц. Прошивка считает статистику: последняя дельта, min/max/средняя,
число неожиданных скачков, измеренная частота. Именно iTOW доказывает
реальную частоту — CFG-VALSET может вернуть ACK, но частоту не дать.

### Дисплей — 3 страницы, ротация каждые 4 секунды

- **GNSS M10**: FIX (NO FIX/DR/2D/3D/G+DR/TIME), SV, RATE (измеренная),
  SPD, sAcc, PPS, счётчики UBX.
- **POSITION**: lat/lon (1e-7°), hAcc/vAcc в метрах, pDOP, спутники
  видимые/используемые.
- **DIAGNOSTICS**: UBX ok, crc err, resync, период PPS (мкс), дельта
  iTOW, версия прошивки приёмника.

### Консоль — раз в секунду, а не на каждый пакет

Каждую секунду печатается сводка: fixType, gnssFixOK, SV, gSpeed, sAcc,
hAcc, iTOW, измеренная rate, счётчики UBX/NMEA. Это сознательно: при
20 Гц печать каждого пакета завалила бы UART-монитор и мешала таймингу.

### Что реализовано, но ещё НЕ активировано (для следующих фаз)

- `ubx_set_uart_baud(115200)` — PHASE E, требует осторожного перехода;
- `ubx_set_rate(100)` — 10 Гц (PHASE F);
- `ubx_set_constellations()` — GPS+Galileo для 20 Гц (PHASE G), GPS-only
  для 25 Гц (PHASE I). По даташиту M10: до 25 Гц один GNSS, до 20 Гц два
  GNSS, до 10 Гц три — поэтому 20 Гц = GPS+Galileo, 25 Гц = GPS-only;
- `ubx_disable_nmea_output()` — PHASE H;
- `ubx_enable_nav_sat()` + разбор NAV-SAT (видимые/используемые спутники,
  C/N0, элевация, азимут) — PHASE J.

### Что проверить на железе после прошивки v0.2

1. В консоли: строки `extension : PROTVER=34.xx` — какой реально протокол.
2. Сводка «GNSS configuration:» — какие созвездия включены с завода.
3. `NAV-PVT output enabled on UART1 (RAM layer)` + ACK.
4. Раз в секунду: `fixType=... SV=... gSpeed=... rate=...`.
5. На экране: страница GNSS M10 с FIX/SV/RATE/SPD, ротация страниц.
6. PPS: строки `PPS edge #N, period ... us` — период должен быть ~1000000 мкс.

Если всё это есть — точка контроля пройдена, и можно переходить к
PHASE E (115200) и дальше к 10/20 Гц. Прошивка: `build_idf.bat -p COM5 flash monitor`.

---

## Этап 2.1: исправление двух багов после первого теста UBX

### Баг 1: синхронные ожидания блокировали приём UART

Симптом: MON-VER/VALGET/ACK падали по таймауту, но потом данные всё же
приходили. Причина: стартовая последовательность (MON-VER, VALGET,
VALSET) выполнялась ВНУТРИ задачи gnss_rx — пока она ждала ответ,
uart_read_bytes() не работал, и ответы копились в кольцевом буфере.

Исправление — ровно один потребитель UART:
- задача `gnss_rx` теперь НИКОГДА не ждёт ответов: только читает UART,
  кормит парсер и диспетчеризует фреймы;
- появился отдельный поток `gnss_init` со стартовой последовательностью;
- синхронизация — через task notifications (FreeRTOS): ожидающий код
  «взводит» ожидание (ubx_resp_arm) ДО отправки команды, спит, а
  диспетчер в gnss_rx будит его при приходе нужного фрейма;
- сопоставление ответов строгое: ACK-ACK (0x05/0x01) и ACK-NAK
  (0x05/0x00) матчатся по классу/ID команды в полезной нагрузке;
  MON-VER ждёт именно фрейм 0x0A 0x04, VALGET — фрейм 0x06 0x8B,
  а не ACK.

### Баг 2: подозрение на неверный hAcc

hAcc ≈ 1656278656 (0x62C0xxxx) — явно мусор. Оффсет 40 по официальной
структуре NAV-PVT верен, длина payload должна быть ровно 92 байта.
Для диагностики добавлен одноразовый DEBUG-дамп первого NAV-PVT:
полная hex-свалка payload по 16 байт со смещениями + все декодированные
поля + сырые байты 40..43 и 68..71. Если длина окажется не 92 или байты
40..43 не похожи на миллиметры (реальный hAcc обычно 1000..100000),
сравним дамп с оффсетами и найдём реальную структуру.

Дополнительно: декодер теперь требует точную длину 92 и логирует
фактическую, если она другая.

### Порядок при старте теперь такой

1. gnss_rx стартует и остаётся активным.
2. gnss_init: MON-VER → печатаем sw/hw/PROTVER.
3. CFG-VALGET → печатаем сводку.
4. NAV-PVT включён? (pvt_rate>0) → пропускаем VALSET; иначе включаем и
   ждём настоящий ACK-ACK.
5. Ждём первый NAV-PVT → DEBUG-дамп (один раз) → непрерывный приём.
6. Каждую секунду: fixType/fixOK/SV/gSpeed/hAcc/vAcc/sAcc/iTOW/rate/
   dITOW и счётчики UBX. Ожидаем dITOW=1000, rate≈1.00.

---

## Этап 2.2: разбор дампа NAV-PVT и исправление парсера CFG-VALGET

Тест v0.2.1 подтвердил: архитектура починена (MON-VER OK, ACK-ACK
приходит, VALGET отвечает), dITOW ровно 1000 мс, crc ошибок нет.

### NAV-PVT декодируется ВЕРНО — «странный» hAcc оказался нормой

Hex-дамп подтвердил оффсеты официально:
- +00: D0B63219 → iTOW=422754000 — совпало с логом байт-в-байт
- +04: EA07 → год 2026, 15:19:24 UTC
- +32 и +36: 7FFFFFFF — валидные маркеры «нет данных» для height/hMSL
- +76: pDOP=9999 — документированный «неизвестный DOP»
- +72: headAcc=18000000 = 180° — «направление неизвестно»
- +68: sAcc=999000 — «точность скорости неизвестна»

Вывод: при fixType=0 (нет фикса) u-blox заполняет поля позиции,
скорости и точностей «растущими» заглушками — поэтому hAcc/vAcc
росли каждую секунду. Это НЕ баг прошивки. Декодер теперь требует
точную длину 92 байта; при отсутствии фикса (fixType<2) консоль
печатает «NO FIX SV=n …» вместо фиктивных чисел, а с фиксом покажет
реальные hAcc/vAcc/sAcc/pDOP.

### Настоящий найденный баг: разбор CFG-VALGET

Размер значения кодируется в СТАРШЕЙ тетраде key ID (биты 31..28):
0x0/1/2 = 1 байт, 0x3 = 2, 0x4 = 4, 0x5 = 8. Я читал биты 22..20 —
из-за этого все ключи после первого парсились со сдвигом, откуда
baud=0, meas=232, все созвездия=0. Доказательство от железа: ответ
на 21 ключ = ровно 114 байт (4 заголовка + 4 baud + 2×2 meas/nav +
18×1 остальные) — значит модуль отвечает правильно, а парсер был
неправильный. После исправления сводка конфигурации должна показать
UART1 baud=38400, meas=1000 мс, rate=1.00 Hz, GPS/GAL/GLO/BDS по
факту, и условие «NAV-PVT уже включён» начнёт срабатывать — лишний
VALSET пропадёт.

### Что смотреть после прошивки v0.2.2

1. Сводка «GNSS configuration:» теперь с реальными числами:
   baud=38400, Measurement=1000 ms, Effective rate=1.00 Hz,
   GPS/GAL/GLO/BDS = заводские значения (не нули).
2. «NAV-PVT already enabled, skipping VALSET» вместо повторного VALSET.
3. «GNSS: NO FIX SV=0 …» без растущего hAcc; на улице — полная строка
   с fixType=2/3 и адекватными hAcc (обычно 1000..50000 мм).
4. MON-VER: ROM SPG 5.10, PROTVER=34.10 — подтверждено, документ UBX-21035062 соответствует.

---

## Этап 2.3: NAV-SAT диагностика спутников + честная статистика PPS

### NAV-SAT — что это и зачем

NAV-SAT — сообщение, в котором приёмник перечисляет ВСЕ спутники, о
которых знает: созвездие (gnssId), номер спутника (svId), силу сигнала
C/N0 в дБ-Гц, элевацию (высоту над горизонтом), азимут (направление на
горизонте) и флаг svUsed — «этот спутник участвует в навигационном
решении». Мы включили его вывод на UART1 (RAM-слой, если он уже не был
включён — VALSET пропускается). Скорость: он приходит на каждую
навигационную эпоху, т.е. сейчас 1 раз в секунду.

Расшифровка gnssId (по документации M10): 0=GPS, 1=SBAS, 2=Galileo,
3=BeiDou, 5=QZSS, 6=GLONASS, 7=NavIC.

Что считается автоматически при каждом приёме:
- visible — сколько спутников слышно (C/N0 > 0);
- used — сколько реально используется в решении;
- разбивка «used/visible» по каждому созвездию;
- средний и максимальный C/N0 среди ИСПОЛЬЗУЕМЫХ спутников — это
  качество решения: 35-40 дБ-Гц отлично, 25-30 терпимо, ниже 20 плохо.

В консоли раз в 10 секунд строка вида:
    SAT: vis=14 used=9 avgCNO=32 maxCNO=44 [GPS 5/7 GAL 4/7]

Зачем это всё: при размещении антенны в машине мы будем смотреть,
какие созвездия и с каких направлений ловятся — и выбрать место.

### Новая страница SIGNAL на OLED

Экран теперь листает 4 страницы (каждые 4 секунды): GNSS M10,
POSITION, **SIGNAL** (новая), DIAGNOSTICS. На странице SIGNAL:
vis/used, таблица созвездий «used/visible» по две на строку, средний и
максимальный C/N0 используемых спутников.

### Честная статистика PPS (gap-aware)

Раньше период между импульсами просто писался в min/max — при паузе в
9 секунд в статистику попадал «период 9000000 мкс», как будто это
нормальное измерение. Теперь ISR классифицирует каждый интервал:
- в пределах ±50 мс от 1 000 000 мкс — здоровый такт, идёт в min/max;
- примерно целое число секунд N>=2 — засчитано N-1 ПРОПУЩЕННЫХ
  импульсов (счётчик missed) и 1 «разрыв» (gap);
- прочие интервалы — 1 пропуск.

На дисплее PPS теперь три состояния: OK (импульсы идут, последний
период ~1 с), INT (были, но интервал не ~1 с — прерывисто), NO (не было
ни одного). На странице DIAGNOSTICS виден счётчик PPS miss. В консоли
при каждом новом пропуске печатается предупреждение.

Бод и навигационная частота НЕ менялись: 38400, 1 Гц, NMEA+NAV-PVT+NAV-SAT.
